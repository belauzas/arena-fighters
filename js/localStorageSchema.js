// currently logged in user
const arenaCurrentUser = '' || 'Username';

// list of registered users
const arenaUsers = ['Username', 'Username2', 'Username3'];

// separate object for each user
const arenaUserUsername = {
    username: 'Username',
    currentPage: 'page'
}